sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t paperplanemove/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true paperplanemove/react-test npm test

after_success:
  - docker build -t paperplanemove/multi-client ./client
  - docker build -t paperplanemove/multi-nginx ./nginx
  - docker build -t paperplanemove/multi-server ./server
  - docker build -t paperplanemove/multi-worker ./worker
  # Log in to the docker CLI, $DOCKER_ID and $DOCKER_PASSWORD are entered in travis-ci.com as Environment variables
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push paperplanemove/multi-client
  - docker push paperplanemove/multi-nginx
  - docker push paperplanemove/multi-server
  - docker push paperplanemove/multi-worker
